import "../styles/sidebar.css"
import "../styles/loader.scss"
import '../components/cameraView/CameraView.css';

const PoseNetApp = ({ Component, pageProps }) => <Component {...pageProps} />

export default PoseNetApp;
